import React from "react"

// Simple “guitar-pick” style badge using CSS – replace with your SVG/logo later.
export default function BadgeVerified() {
  return (
    <span className="inline-flex items-center gap-1 text-xs font-bold text-black bg-green-400 px-2 py-1 rounded-full">
      ✔ Verified
    </span>
  )
}
